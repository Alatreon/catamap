<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/emptyStateContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp"
    android:background="@android:color/black"
    android:visibility="gone">

    <!-- Icône carte -->
    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_map"
        android:contentDescription="@string/no_map_icon_description"
        android:layout_marginBottom="24dp"
        app:tint="#808080" />

    <!-- Message principal -->
    <TextView
        android:id="@+id/emptyStateText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/onboarding_slide3_description"
        android:textColor="#E0E0E0"
        android:textSize="16sp"
        android:lineSpacingMultiplier="1.5"
        android:gravity="center"
        android:fontFamily="sans-serif"
        android:layout_marginBottom="32dp" />

    <!-- Bouton optionnel pour ouvrir le gestionnaire -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/openMapManagerButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/open_map_manager"
        android:textSize="16sp"
        android:paddingHorizontal="24dp"
        android:paddingVertical="12dp"
        style="@style/Widget.Material3.Button" />

</LinearLayout>